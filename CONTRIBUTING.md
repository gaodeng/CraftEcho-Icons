# Contributing

感谢你帮助完善 CraftEcho Icons。

## 可以贡献什么

- 修正 `appfilter.xml` 中错误或过时的应用组件映射
- 修正 `drawable.xml` 中的图标名称
- 修复 Android 工程的构建或兼容性问题
- 在拥有相应权利的前提下补充、修复或重绘图标

## 提交要求

1. 一个提交尽量只处理一类问题，并在说明中列出受影响的应用或图标。
2. drawable 文件名只使用小写英文字母、数字和下划线。
3. 同步维护 `app/src/main/assets/` 与 `app/src/main/res/xml/` 中对应的映射文件。
4. 提交前运行 `./gradlew assembleDebug`，确认工程可以构建。
5. 不要提交密钥、签名文件、账号凭据或来源不明的素材。

提交图标即表示你确认自己有权贡献该素材，并允许项目在适用法律及仓库声明允许的范围内保存和分发它。

